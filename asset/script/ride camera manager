using UnityEngine;

public class RideCameraManager : MonoBehaviour
{
    public Camera vrCamera;
    public Transform cart;
    public Vector3 offset;

    void LateUpdate()
    {
        if (cart == null) return;

        vrCamera.transform.position = cart.position + offset;
        vrCamera.transform.rotation = cart.rotation;
    }
}
